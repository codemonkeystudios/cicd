#########################################
# Production Environment (.env.production)
# ----------------------------------------
# This file configures your production environment.
# DO NOT hardcode secrets in this file; load them via a secrets manager.
#########################################

# Application Settings
APP_ENV=production
APP_DEBUG=false            # Debugging must always be disabled in production
APP_URL=https://www.example.com

# Database Settings (MySQL)
DB_CONNECTION=mysql
DB_HOST=prod-db.example.com
DB_PORT=3306
DB_DATABASE=cf_production
DB_USERNAME=cf_prod_user
DB_PASSWORD=${PROD_DB_PASSWORD}       # Use secret injection from AWS SSM, Azure Key Vault, etc.

# Redis Settings (for session caching)
REDIS_HOST=prod-redis.example.com
REDIS_PORT=6379

# ColdFusion Administrator
CF_ADMIN_USER=admin
CF_ADMIN_PASSWORD=${PROD_CF_ADMIN_PASSWORD}    # Load securely, never commit secrets
